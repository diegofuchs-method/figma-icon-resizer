{
  "name": "figma-icon-resizer",
  "version": "1.0.0",
  "description": "Create icon component sets from a selected vector object with multiple sizes and stroke weights",
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=code.js --external:figma",
    "watch": "esbuild code.ts --bundle --outfile=code.js --external:figma --watch",
    "dev": "npm run watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "latest",
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
